#include <iostream>
using namespace std;

int main()
{

    // std::cout << "hello jaru karu che have late to late is never late ";
    // std::cout << "hello jaru karu che have late to late is never late ";

    // std::cout << "hello jaru karu che have late to late is never late ";

    // std::cout << "hello jaru karu che have late to late is never late ";

    cout << " std no use karvu hoy to use karo namespace " << endl;  //endl for the new line 
    cout << " std no use karvu hoy to use karo namespace \n";   //new line \n

    cout << " std no use karvu hoy to use karo namespace " << endl;

    cout<<3+5<<endl;

    cout<<"3+5";
};